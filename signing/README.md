# Stable sideload signing for Tank Chief Android APKs.
# Same keystore + higher versionCode = install over the previous APK (no uninstall).
# Password default: tankchief (override with TANKCHIEF_KEYSTORE_PASS).
#
# Modern Android will not install an unsigned release APK. This key is for
# distribution outside the Play Store (GitHub Releases / USB sideload).
