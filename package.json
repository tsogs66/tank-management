{
  "name": "tank-chief",
  "version": "2.1.44",
  "description": "Tank Chief — multi-vessel fuel tank management with sounding calibration, bunkering distribution, voyage fuel planning, and offline sync",
  "author": "ts0gs (Marvin C. Endozo)",
  "main": "desktop/main.js",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node scripts/seed-vessel.js",
    "dev": "node server/index.js",
    "desktop": "electron .",
    "dist:win": "electron-builder --win --publish never",
    "dist:linux": "electron-builder --linux --publish never",
    "stage:runtime": "node scripts/stage-runtime.js",
    "embed": "node scripts/copy-embedded.js",
    "parity": "node scripts/parity-check.js",
    "android:sync": "npm run embed && npx cap sync android && node scripts/apply-android-bridges.js",
    "android:apk": "npm run android:sync && node scripts/android-release-prep.js && cd android && ./gradlew assembleRelease",
    "icons": "node scripts/render-icons.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  },
  "license": "MIT",
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "playwright": "^1.62.1"
  }
}
